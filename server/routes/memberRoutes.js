const express = require("express");

const memberController = require("./../controllers/memberControllers");

const router = express.Router();

// router.get("/:memberId", memberController.getProfile);

module.exports = router;
